node_modules
.next
dist
coverage
.npm-cache
.env
.env.local
*.tsbuildinfo
.DS_Store

# Local data dumps for one-shot imports (keep the real product catalogue
# out of git).
apps/backend/data/
*.xlsx
*.csv
.vercel
.env*.local

# Environment secrets and local backups; keep only example templates.
.env.*
!.env.example
!.env.*.example
